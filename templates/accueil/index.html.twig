{% extends "base.html.twig" %}

{%  block body %}
    <nav class="navbar navbar-expand-md navbar-dark bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app_user_index') }}"><img class="logo" src="{{ asset('img/tokepi_logo_blanc.png') }}" alt=""></a>
            <div class="navbar-collapse">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="">
                            Gestion médicaments
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="">
                            Gestion membres
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" >
                            Gestion messages
                        </a>
                    </li>
                    {% if app.user is null %}
                        <li class="nav-item">
                            <a href="{{ path('app_registrationlog_register') }}" class="nav-link">Inscription</a>
                            {# <a href="{{ path('app_registrationlog_register') }}" data-toggle="modal" data-target="#sem-reg">Inscription</a>#}
                        </li>
                        <li class="nav-item">
                            <a href="{{ path('app_registrationlog_connexion') }}" class="nav-link">Connexion</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a href="{{ path('app_registrationlog_logout') }}" class="nav-link">Deconnexion</a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>
<h1>Ceci est la page accueil</h1>

{% endblock %}